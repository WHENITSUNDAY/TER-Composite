# Configuration pour test composite C/C (Carbone/Carbone)
# Carré avec fibre carbone au centre dans matrice carbone - test de traction

test_type = composite

# Fichier de maillage
mesh_file = ../mesh/composite_simple.msh

# Matériau 1: Matrice carbone (pyrocarbone)
Young_modulus = 20e9       # 20 GPa (carbone moins dense)
Poisson_ratio = 0.25
density = 1900             # kg/m³

# Matériau 2: Fibre carbone haute performance
Young_modulus_fiber = 350e9  # 350 GPa (carbone haut module)
Poisson_ratio_fiber = 0.2
density_fiber = 1800         # kg/m³

# Chargement
force_value = 1000         # Force en N

# Sortie
output_dir = ../results
output_prefix = composite_simple
